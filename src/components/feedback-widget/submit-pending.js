import React from "react"
import WidgetWrapper from "./widget-wrapper"

const SubmitPending = () => (
  <WidgetWrapper className="feedback-success">
    <p>Hold tight! We’re submitting your feedback.</p>
  </WidgetWrapper>
)

export default SubmitPending
